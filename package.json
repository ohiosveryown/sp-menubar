{
  "name": "sp-menubar",
  "version": "1.0.0",
  "description": "Simple menubar app for ShootProof via Electron.",
  "license": "MIT",
  "devDependencies": {
    "electron-packager": "^8.0.0",
    "electron-prebuilt": "^1.4.1"
  },
  "dependencies": {},
  "main": "main.js",
  "scripts": {
    "build-osx": "electron-packager . Example --platform=darwin --arch=x64 --version=0.36.0 --icon=Icon.icns",
    "start": "electron ."
  }
}
